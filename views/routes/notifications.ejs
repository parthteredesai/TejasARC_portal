<% layout("/layouts/boilerplate") %>

<% allData.forEach((data, index) => { 
     const dateObj = new Date(data.insight.timestamp);
     const date = dateObj.toLocaleDateString("en-IN", {
       day: "2-digit",
       month: "short",
       year: "numeric"
     });
     const time = dateObj.toLocaleTimeString("en-IN", {
       hour: "2-digit",
       minute: "2-digit",
       hour12: true
     });
%>
<div class="notifcover">
  <div class="notif">
    <ul>
      <li>
        <strong>Major Factor:</strong>
        <%= data.insight.live_diagnosis.root_cause %>
      </li>
      <li>
        <strong>Recommendation:</strong>
        <%= data.insight.weather_benchmark.recommendation %>
      </li>
      <li>
        <strong>Action:</strong>
        <%= data.insight.live_diagnosis.action %>
      </li>
    </ul>
    <h6 class="notif-time">
       <%= date %>
       <%= time %>
    </h6>
  </div>
</div>
<% }) %>
